# Pre-Publish Patch Notes

Applied from Claude's adversarial publish review:

- BLOCKER B1: added `## Honest limitation` to README.
- BLOCKER B2: added `## How to try it` with StudioNet GEN requirement, user-owned mandate funding, and `withdraw_unused` recovery stated up front.
- LOW L1: documented that mandate terms are immutable after creation.
- MEDIUM M1: added `.env.example` matching `src/lib/config.ts`.
- Fixed the unclosed JSON code fence at the end of the uploaded README.
- Added `PUBLISH_FORM.md` with the reviewed publish copy.

Not fabricated / not claimed:

- No transaction hashes were added to `TESTING.md` because the reviewed repository did not contain them.
- No frontend transaction logic was changed in this patch.
